import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const CRUD = () => {
    return (
        <View>
            <Text>HAI</Text>
        </View>
    )
}

export default CRUD

const styles = StyleSheet.create({})
